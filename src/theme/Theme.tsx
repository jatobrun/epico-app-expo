import { DefaultTheme } from 'react-native-paper';

export const ThemePaper = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        primary: '#070981',
        accent: '#78C4F1',
    },
};